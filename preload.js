// Preload script for Electron security
// Currently empty - add APIs here if you need to expose Node features to the renderer
// For now we use HTTP calls to our Express server, so this stays minimal